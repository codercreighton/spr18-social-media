<p id="notice"><%= notice %></p>

<p>
  <strong>Message:</strong>
  <%= @tweet.message.html_safe %>
</p>

<p>
 <strong>User:</strong>
  <%= link_to @tweet.user.username, show_user_path(id: @tweet.user_id) %>
</p>

<% if current_user.id == @tweet.user_id %>| <%= link_to 'Edit', edit_tweet_path(tweet) %> | <%= link_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' } %><% end %></p>
    </div>